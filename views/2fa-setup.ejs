<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Setup 2FA — Breasy Sales Machine</title>
  <link rel="icon" type="image/svg+xml" href="/img/favicon.svg">
  <link rel="stylesheet" href="/css/style.css">
  <script>
    (function() {
      var t = localStorage.getItem('breasy-theme') || 'dark';
      document.documentElement.setAttribute('data-theme', t);
    })();
  </script>
  <style>
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      background: var(--bg);
      padding: 20px;
    }
    .setup-container {
      width: 100%;
      max-width: 440px;
    }
    .setup-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 36px 32px 32px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.2);
    }
    .setup-logo {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      margin-bottom: 24px;
    }
    .setup-logo svg {
      filter: drop-shadow(0 2px 8px rgba(59,130,246,0.3));
    }
    .setup-logo-text {
      font-size: 24px;
      font-weight: 800;
      color: var(--text);
      letter-spacing: -0.5px;
    }
    .setup-logo-sub {
      font-size: 10px;
      font-weight: 600;
      color: var(--text-dim);
      letter-spacing: 2px;
      text-transform: uppercase;
    }
    .setup-title {
      text-align: center;
      font-size: 18px;
      font-weight: 700;
      color: var(--text);
      margin-bottom: 6px;
    }
    .setup-desc {
      text-align: center;
      font-size: 13px;
      color: var(--text-dim);
      margin-bottom: 24px;
      line-height: 1.5;
    }
    .qr-wrap {
      text-align: center;
      margin-bottom: 20px;
      background: #fff;
      border-radius: 12px;
      padding: 16px;
      display: inline-block;
      margin-left: auto;
      margin-right: auto;
      width: 100%;
    }
    .qr-wrap img {
      max-width: 200px;
      height: auto;
    }
    .secret-key {
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 10px 14px;
      text-align: center;
      font-family: monospace;
      font-size: 15px;
      font-weight: 700;
      letter-spacing: 3px;
      color: var(--accent);
      margin-bottom: 8px;
      word-break: break-all;
    }
    .secret-label {
      font-size: 11px;
      color: var(--text-dim);
      text-align: center;
      margin-bottom: 20px;
    }
    .steps {
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 14px 16px;
      margin-bottom: 20px;
      font-size: 13px;
      color: var(--text-dim);
      line-height: 1.8;
    }
    .steps strong { color: var(--text); }
    .setup-card .form-group {
      margin-bottom: 16px;
    }
    .setup-card .form-group label {
      display: block;
      margin-bottom: 6px;
      font-size: 13px;
      font-weight: 600;
      color: var(--text-dim);
    }
    .setup-card input {
      width: 100%;
      padding: 12px 14px;
      font-size: 20px;
      font-weight: 700;
      text-align: center;
      letter-spacing: 8px;
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: 8px;
      color: var(--text);
    }
    .setup-card input:focus {
      outline: none;
      border-color: var(--accent);
      box-shadow: 0 0 0 3px rgba(59,130,246,0.15);
    }
    .setup-btn {
      width: 100%;
      padding: 12px;
      font-size: 15px;
      font-weight: 700;
      color: #fff;
      background: linear-gradient(135deg, #3b82f6, #8b5cf6);
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: opacity 0.2s, transform 0.1s;
    }
    .setup-btn:hover { opacity: 0.9; }
    .setup-btn:active { transform: scale(0.98); }
    .setup-error {
      background: var(--error-bg);
      border: 1px solid var(--error-border);
      color: var(--red);
      padding: 10px 14px;
      border-radius: 8px;
      font-size: 13px;
      margin-bottom: 16px;
      text-align: center;
    }
    .setup-footer {
      text-align: center;
      margin-top: 20px;
      font-size: 12px;
      color: var(--text-dim);
    }
  </style>
</head>
<body>
  <div class="setup-container">
    <div class="setup-card">
      <div class="setup-logo">
        <svg viewBox="0 0 32 32" fill="none" width="40" height="40">
          <rect width="32" height="32" rx="8" fill="url(#lg)"/>
          <path d="M8 8h7c2.8 0 4.8 1.5 4.8 3.8 0 1.6-1 2.8-2.4 3.3v.2c1.8.5 2.9 1.8 2.9 3.7 0 2.7-2.2 4.2-5.3 4.2H8V8zm3.4 6h2.8c1.5 0 2.3-.7 2.3-1.8 0-1.2-.9-1.9-2.4-1.9h-2.7v3.7zm0 6.7h3.2c1.6 0 2.5-.8 2.5-2.1 0-1.3-1-2.1-2.7-2.1h-3V20.7z" fill="#fff"/>
          <defs><linearGradient id="lg" x1="0" y1="0" x2="32" y2="32" gradientUnits="userSpaceOnUse"><stop offset="0%" stop-color="#3b82f6"/><stop offset="100%" stop-color="#8b5cf6"/></linearGradient></defs>
        </svg>
        <div>
          <div class="setup-logo-text">Breasy</div>
          <div class="setup-logo-sub">Sales Machine</div>
        </div>
      </div>

      <div class="setup-title">Setup Two-Factor Authentication</div>
      <div class="setup-desc">Scan the QR code below with Google Authenticator, then enter the 6-digit code to activate.</div>

      <div class="steps">
        <strong>1.</strong> Open Google Authenticator on your phone<br>
        <strong>2.</strong> Tap <strong>+</strong> then <strong>Scan QR code</strong><br>
        <strong>3.</strong> Point camera at the QR code below<br>
        <strong>4.</strong> Enter the 6-digit code shown in the app
      </div>

      <div class="qr-wrap">
        <img src="<%= qrDataUrl %>" alt="Scan this QR code">
      </div>

      <div class="secret-key"><%= secret %></div>
      <div class="secret-label">Manual entry key (if you can't scan)</div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="setup-error"><%= error %></div>
      <% } %>

      <form method="POST" action="/2fa/setup">
        <div class="form-group">
          <label for="token">Enter 6-digit code from app</label>
          <input type="text" id="token" name="token" maxlength="6" pattern="[0-9]{6}" inputmode="numeric" autocomplete="one-time-code" placeholder="000000" required autofocus>
        </div>
        <button type="submit" class="setup-btn">Activate 2FA</button>
      </form>
    </div>
    <div class="setup-footer">Breasy Sales Machine &copy; 2026</div>
  </div>
</body>
</html>
